{
  "name": "query-info",
  "version": "1.0.0",
  "description": "Queries some specific Substrate block info on a timer",
  "main": "index.js",
  "scripts": {
    "start": "node ./index.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@polkadot/api": "^2.10.1"
  }
}
